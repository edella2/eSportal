require 'rails_helper'

RSpec.describe Stream, type: :model do
  pending 'is invalid without a url' do
  end

  pending 'is invalid without a language' do
  end

  pending 'is associated with a tournament' do
  end

  describe '::update_or_create' do
    pending 'only adds new streams to the database' do
    end
  end
end
